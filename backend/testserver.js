// import express from 'express';
// import products from './routes/productsRoutes.js';
// import user from './routes/userRoutes.js';
// import order from './routes/orderRoutes.js';
// import payment from './routes/paymentRoutes.js';

// const app = express();
// app.use(express.json());

// // Now it's safe to call
// console.log('Products route paths:', products.stack.map(l => l.route?.path));
// console.log('Users route paths:', user.stack.map(l => l.route?.path));
// console.log('Orders route paths:', order.stack.map(l => l.route?.path));
// console.log('Payment route paths:', payment.stack.map(l => l.route?.path));

// // Register routers
// app.use('/api/v1', products);
// app.use('/api/v1', user);
// app.use('/api/v1', order);
// app.use('/api/v1', payment);

// app.listen(5000, () => console.log('Server running'));
